<?php

namespace App\Http\Controllers;

use Illuminate\Http\Request;

class PostsController extends Controller
{
    //index() = List of all posts
    //create() = Will represent the create form
    //store() = create function will submit data to this store function
    //          will take care of submitting it to the model and to the database
    //edit() = edit form
    //update() = will receive data from edit fuction and will take care of 
    //           updating data to database
    //show() = will only show single post
    //destroy() = take care of deleting

}
