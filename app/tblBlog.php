<?php

namespace App;

use Illuminate\Database\Eloquent\Model;

class tblBlog extends Model
{
    // When a model is created, tblBlog in this example, by default the table name is tblBlogs.
    // But if you wanted to change that, you could. You can do it by the following below:
        // Table Name
        ////// protected $table = "tblBlogs";
        // Primary Key
        ////// protected $primaryKay = "id";
        // Timestamps
        // public $timestamps = true;
}
